import javax.swing.*;import java.util.Arrays;public class Arreglos {    public static void main(String args[]) {        int numeros[] = new int[5];        String palabras[]= new String[5];        int numerotes[]=new int[5];        int i;        for (i = 0; i < numeros.length; i++) {            numeros[i]=Integer.parseInt(JOptionPane.showInputDialog("Ingrese el numero en el arreglo"));        }        for(i=0;i<numeros.length;i++){            System.out.println("Los daots originales son: "+numeros[i]);        }        System.out.println("Los datos ordenados son: ");        Arrays.sort(numeros);        for (i=0;i<5;i++){            System.out.println("Los datos ordenados son: "+numeros[i]);        }        //rellenando al arreglo palabras        Arrays.fill(palabras, "Bienvanido a la fiesta");        System.out.println("Elementos del arreglo palabras: ");        for (i=0;i<palabras.length;i++){            System.out.println("Arreglo palabras: "+palabras[i]);        }        //Copiando los elementos del arreglo numeros a numerotes        System.arraycopy(numeros,0,numerotes,0,numerotes.length);        for (i=0;i<palabras.length;i++){            System.out.println(" Numerotes: "+numerotes[i]);        }    }}